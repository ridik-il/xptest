# name: "PR Validation (Stages 1+2)"

# on:
#   pull_request:
#     branches: [main]

# permissions:
#   contents: read

# jobs:
#   validate:
#     name: "Validate Compositions (Layers 1-3)"
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.12"

#       - name: Install xptest
#         run: |
#           cd xptest
#           pip install -e ".[dev]"

#       - name: Install OPA
#         run: |
#           curl -L -o /usr/local/bin/opa \
#             https://openpolicyagent.org/downloads/v1.4.2/opa_linux_amd64_static
#           chmod +x /usr/local/bin/opa
#           opa version

#       - name: Lint (ruff)
#         run: |
#           cd xptest
#           ruff check src/ tests/

#       - name: Run tests
#         run: |
#           cd xptest
#           pytest tests/ -v --tb=short

#       - name: Validate compositions
#         run: |
#           cd xptest
#           for comp in fixtures/valid/*.yaml fixtures/dep-errors/*.yaml fixtures/policy-violations/*.yaml; do
#             [ "$(basename "$comp")" = "xrd.yaml" ] && continue
#             echo "--- Validating: $comp ---"
#             xptest validate \
#               --composition "$comp" \
#               --xrd fixtures/xrd.yaml \
#               --config xptest.yaml \
#               --output /dev/null || true
#           done
